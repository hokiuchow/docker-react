sudo: required
services:
  - docker

before_install:
  - docker build -t hokiuchow/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true hokiuchow/docker-react npm run test -- --coverage
